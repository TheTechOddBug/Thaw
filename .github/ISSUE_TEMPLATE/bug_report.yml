name: Bug Report
description: File a bug report
title: "[Bug] "
type: bug
projects: ["thaw-app/1"]
body:
  - type: markdown
    attributes:
      value: |
        Check [Frequent Issues](https://github.com/thaw-app/Thaw/blob/development/FREQUENT_ISSUES.md) and search [open and closed issues](https://github.com/thaw-app/Thaw/issues?q=is%3Aissue) first.

        > [!IMPORTANT]
        > macOS 27 (Golden Gate) support, preview builds, and "it doesn't work on 27" all belong in [#687](https://github.com/thaw-app/Thaw/issues/687). We close new issues about that as duplicates.

  - type: checkboxes
    id: checks
    attributes:
      label: Before filing
      options:
        - label: If this is about macOS 27, I read #687 and this is a specific bug it does not already cover
          required: true

  - type: textarea
    id: description
    attributes:
      label: What happened?
      description: What went wrong, and what you expected instead.
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
      placeholder: |
        1.
        2.
        3.
    validations:
      required: true

  - type: upload
    id: evidence
    attributes:
      label: Logs, screenshots, or a screen recording
      description: |
        Attach at least one. We can rarely do anything with a report that gives us nothing to look at.

        A screenshot or a short recording covers most visual bugs. If the app crashes, freezes, or the problem looks timing related, a diagnostic log helps far more: Settings, Advanced, Diagnostics, turn on diagnostic logging, reproduce the bug, then attach the log.
    validations:
      required: true

  - type: input
    id: app_version
    attributes:
      label: Thaw version
      placeholder: e.g. 2.1.0
    validations:
      required: true

  - type: input
    id: macos_version
    attributes:
      label: macOS version
      placeholder: e.g. 26.1 (Tahoe)
    validations:
      required: true

  - type: dropdown
    id: monitor_setup
    attributes:
      label: Display setup
      description: Optional. Plenty of layout and notch bugs only happen on one kind of setup.
      options:
        - Single display
        - Multiple displays
    validations:
      required: false

  - type: textarea
    id: additional
    attributes:
      label: Anything else?
      description: Configuration, other menu bar apps, when it started happening.
    validations:
      required: false
